# Lab 08 — SQL Proxy Switchover
# Override defaults by copying to .env and editing.

# Probe settings
DURATION=30          # Total probe duration (seconds)
INTERVAL=0.5         # Probe interval (seconds)
PRE_SWITCHOVER=10    # Seconds before stopping tidb-1
